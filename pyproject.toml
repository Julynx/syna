[project]
name = "syna"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Julynx", email = "julicabria@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "openrouter>=1.1.137",
    "python-dotenv>=1.2.3",
    "string-grab>=1.3.0",
    "bollard>=0.1.3",
]

[project.scripts]
syna = "syna:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
